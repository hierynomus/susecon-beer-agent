apiVersion: v1
kind: Service
metadata:
  name: {{ include "susecon-beer-agent.fullname" . }}
  labels:
    {{- include "susecon-beer-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "susecon-beer-agent.selectorLabels" . | nindent 4 }}

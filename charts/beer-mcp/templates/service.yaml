apiVersion: v1
kind: Service
metadata:
  name: {{ include "beer-mcp.fullname" . }}
  labels:
    {{- include "beer-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beer-mcp.selectorLabels" . | nindent 4 }}
